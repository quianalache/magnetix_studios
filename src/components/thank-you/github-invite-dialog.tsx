// Stub — see publish/README.md.
export function GithubInviteDialog(_props?: {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  sessionId?: string;
  token?: string;
}) {
  return null;
}
